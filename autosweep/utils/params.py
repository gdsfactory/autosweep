import logging

# Logging settings
logger_format = r"%(asctime)s [%(levelname)s] %(name)s: %(message)s"
logger_level = logging.INFO

# The datetime format string used in the archive folder name
datetime_frmt = r"%Y%m%d-%H%M%S"
